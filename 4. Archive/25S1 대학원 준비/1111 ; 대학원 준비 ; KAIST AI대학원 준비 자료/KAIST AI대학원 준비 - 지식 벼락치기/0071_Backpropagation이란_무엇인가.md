# 🧐 Backpropagation이란 무엇인가?

## 정답 링크

https://velog.io/@jhlim2993/Deep-Learning-%EA%B8%B0%EC%B4%88-%EB%82%B4%EC%9A%A9-%EC%A0%95%EB%A6%AC

## 형식적 답

output layer에서 반대로 hidden layer, input layer에까지 손실 비용이 전달되어 파라미터를 업데이트 하는 학습 과정.

## AI 답변

백프로퍼게이션(Backpropagation)은 인공신경망(Neural Network)에서 사용되는 가장 중요한 학습 알고리즘 중 하나입니다. 이는 오차 역전파(Error Backpropagation) 또는 역전파(Backprop)라고도 불립니다.

백프로퍼게이션은 신경망이 학습 데이터에 대해 잘못 예측한 출력값과 실제 값 사이의 차이인 손실(Loss)을 최소화하기 위해 가중치(Weight)와 편향(Bias)을 자동으로 조정하는 방법입니다. 

구체적인 과정은 다음과 같습니다:

1. 입력 데이터를 신경망에 전달하여 출력값을 계산합니다.

2. 출력값과 실제 값 사이의 오차(손실)를 계산합니다.

3. 오차를 출력층에서부터 역방향(역전파)으로 전파시켜 각 층의 가중치와 편향에 대한 기울기(Gradient)를 계산합니다.

4. 기울기 값에 따라 가중치와 편향을 업데이트하여 다음 반복에서 오차가 줄어들도록 조정합니다.

5. 이 과정을 반복적으로 수행하여 신경망의 가중치와 편향을 최적화합니다.

백프로퍼게이션은 경사 하강법(Gradient Descent)의 일종으로, 손실 함수를 최소화하는 방향으로 가중치와 편향을 업데이트합니다. 이를 통해 신경망이 점점 더 정확한 예측을 할 수 있도록 학습됩니다.

