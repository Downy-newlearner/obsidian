---
created: 2025-09-16
Question: VGG에서 3x3 filters만 사용한 특별한 이유가 있을까?
---
네, 아주 중요한 포인트예요 👍 VGG가 **3×3 필터만 사용**한 데에는 몇 가지 의도가 있습니다.

---

### **1. 더 깊게 쌓기 위한 설계 철학**

- 큰 커널(예: 7×7, 11×11)은 한 번에 많은 정보를 보지만, **모델이 얕아지고 파라미터가 많아집니다.**
    
- 대신 3×3 필터를 여러 번 쌓으면 같은 **수용 영역(receptive field)** 을 확보하면서도 네트워크를 깊게 만들 수 있습니다.
    
    - 예: 3×3 conv 두 번 = 5×5 receptive field
        
    - 3×3 conv 세 번 = 7×7 receptive field
        


---

### **2. 파라미터 수 절감**

- 7×7 conv 한 장의 파라미터 수 = 7 \times 7 \times C_{in} \times C_{out}
    
- 3×3 conv 세 장의 파라미터 수 = 3 \times 3 \times C_{in} \times C_{mid} + 3 \times 3 \times C_{mid} \times C_{mid} + 3 \times 3 \times C_{mid} \times C_{out}
    
- 결과적으로 **더 적은 파라미터**로 **같은 수용 영역**을 구현할 수 있습니다.
    

---

### **3. 비선형성(Non-linearity) 증가**

- Conv마다 ReLU를 붙이므로, 3×3을 여러 번 쌓으면 **비선형성이 누적**됩니다.
    
- 즉, 더 복잡한 함수 근사가 가능해져 **표현력이 증가**합니다.
    

---

### **4. 단순화된 구조 철학**

- “작은 필터 + 깊은 구조”라는 단순한 규칙은 설계와 재현성이 쉽습니다.
    
- 그래서 VGG는 **매우 직관적이면서 강력한 baseline 모델**이 되었고, 이후 연구(ResNet, DenseNet 등)의 출발점이 되었어요.
    

---

👉 정리하면, VGG가 3×3 필터만 사용한 이유는 **효율성(적은 파라미터)**, **표현력 증가(비선형성 누적)**, **단순하고 깊은 구조** 때문입니다.

  

혹시 제가 **3×3 conv 여러 장 vs 큰 커널 한 장의 receptive field와 파라미터 차이**를 수식과 그림으로 비교해드릴까요?