Lecture materials: list

"? - playsAirGuitar(jody)" 이건 쿼리이다.

데이터베이스와 쿼리를 연결하는 것은 
프로시저로 하는 경우 Embedded SQL이라고 한다.
	접근 허락을 먼저 받은 후 
	DB에 접근해서 가져올 SQL을 만들어야한다.
	SQL을 DB에서 실행시켜야함
	DB에서 스트림(대부분 문자열)으로 데이터를 받는다.

==Close-world assumption==
데이터베이스에 있는 것들은 참을 판별 가능, 없는 것들은 모르는 것(거짓)이다.


## Knowledge Base 1, 2, 3, 4
데이터가 많아질 수록 Search time은 증가할 것이다.
즉, Search Complexity가 증가한다. -> 정렬을 하고 Binary Search를 하면 더 빠르게 검색할 수 있다.

또는,
1. 링크드 리스트
2. arg 수로 확인
3. 해싱

playsAirGuitar(vincent):- listen2music(vincent), happy(vincent)
라면  listen2music(vincent), happy(vincent) 둘다 존재해야지 playsAirGuitar(vincent)가 true이다.

if 문에서 ';'(세미콜론)은 or이다. ','은 and이다.

Prolog에서 A :- B 는 "B이면 A이다."(B -> A) 라는 뜻이다.

"?- woman(X)" 에서 woman인 사람을 모두 찾아내기 위해 X를 사용하는 것이다.(Unification)


## Arity
arg의 수
arity를 다르게 하여 다형성을 챙길 수 있다.

---

## Prolog의 cons pairs <- 매우 중요!!!!!!
마치 파이썬의 ``` H,T = L``` 과 같은 연산(일단 이 연산은 없는 연산인데 이런 느낌이다 정도만 언급하심)



### Prolog 코드 확인해보기
Listing 2: member/2(arity = 2라는 뜻)
Listing 3: append/3
Listing 4: Reverse of lists
Listing 5: sum_list/2
-> Prolog는 declarative(?) language라서 새로운 접근 방법을 사용했다.
Listing n: Hanoi tower
Listing 8: factorial
Listing 9: Conditional statement
Listing 10: Loop statement
	Recursive로 정의됨