# 2025 AI Platform - 포트폴리오 서비스 구현

인공지능 기반 포트폴리오 자동 생성 서비스입니다. 텍스트 피드를 분석하여 전문적인 포트폴리오 페이지를 자동으로 생성합니다.

## 🚀 주요 기능

- **텍스트 분석**: Gemini API를 활용한 포트폴리오 내용 분석
- **이미지 매칭**: AI 기반 이미지-텍스트 매칭 시스템
- **자동 포트폴리오 생성**: 분석 결과를 기반으로 HTML 포트폴리오 자동 생성

## 📁 프로젝트 구조

```
.
├── Feed_analysis_module/    # AI 분석 모듈
├── data/                    # 입력 데이터 및 로그
├── portfolio_screen/        # 포트폴리오 UI 템플릿
├── results/                 # 분석 결과 파일
└── main.py                 # 메인 실행 스크립트
```

자세한 구조는 [PROJECT_STRUCTURE.md](./PROJECT_STRUCTURE.md) 참고

## 🔧 설치 및 실행

### 1. 의존성 설치

```bash
pip install -r requirements.txt
```

### 2. 환경 변수 설정

`.env_example` 파일을 참고하여 `.env` 파일을 생성하고 API 키를 설정하세요:

```bash
cp .env_example .env
# .env 파일을 편집하여 API 키 추가
```

필요한 API 키:

- `GOOGLE_API_KEY`: Google Gemini API 키
- `UPSTAGE_API_KEY`: Upstage API 키 (선택사항)

### 3. 실행

```bash
python main.py
```

## 📊 작업 흐름

1. **데이터 입력**: `data/` 디렉토리에 텍스트 또는 JSON 파일 준비
2. **텍스트 분석**: Gemini API로 포트폴리오 내용 분석
3. **이미지 매칭**: 이미지와 텍스트를 매칭
4. **포트폴리오 생성**: HTML 포트폴리오 자동 생성

## 📝 사용법

### 기본 실행

```bash
python main.py
```

스크립트는 다음을 수행합니다:

- `data/백앤드 시나리오.txt` 파일을 읽어서 분석
- 텍스트 분석 결과를 `results/` 디렉토리에 저장
- 이미지 매칭 결과 생성
- 포트폴리오 HTML 생성

### 결과 확인

- **분석 결과**: `results/*_image_matched.json`
- **포트폴리오**: `portfolio_screen/index_result.html`

## 🛠️ 기술 스택

- **Python 3.11+**
- **Google Gemini API**: 텍스트 분석
- **HTML/CSS**: 포트폴리오 UI

## 📄 라이센스

이 프로젝트는 개인/교육 목적으로 사용됩니다.

## 📞 문의

프로젝트 관련 문의사항이 있으시면 이슈를 등록해주세요.
